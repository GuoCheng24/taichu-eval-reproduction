__pycache__/
*.pyc
results/img/

# Merged arm files are a function of the shards, which are tracked:
#   groundwork shard merge 'results/<arm>_s*.jsonl' --expect N -o <out>
# reproduces them exactly, and it refuses if a shard is short or disagrees.
results/cvbench_think_on_FCV.jsonl
results/mathvista_think_on_FMV.jsonl

# `groundwork watch` bookkeeping: pids, local paths and a raw log. Machine
# state, not a result - and the flag file is meant to be read on THIS machine.
archive/runs/
